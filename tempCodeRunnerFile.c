
                // while (1) {
                // ch = _getch();

                // if (ch == 13) {  // Enter key
                // break;
                // }

                // if (ch == 8) {  // Backspace
                // if (amount > 0) {
                //     amount = amount / 10;
                //     printf("\b \b");  // Erase the last character on the screen.
                // }
                // } else if (ch >= '0' && ch <= '9') {
                // amount = amount * 10 + (ch - '0');
                // printf("%c", ch);  // Display the digit.
                // }
                // }